# FastAPI + PostgreSQL CRUD Demo

This project is a simple backend API using **FastAPI**, **SQLAlchemy**, and **PostgreSQL**.  
It allows managing items with basic **CRUD** operations and serves as a demonstration of a clean, modular Python backend architecture.
